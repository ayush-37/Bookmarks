<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= user.name %>'s Library</title>
  <link rel="stylesheet" href="/styles/site_style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">
  <div class="container py-5">
    <a href="/explore" class="btn btn-outline-light mb-4">&larr; Back to Explore</a>

    <div class="d-flex align-items-center mb-4">
      <img src="<%= user.avatar %>" width="64" height="64" class="rounded-circle me-3" alt="<%= user.name %>">
      <div>
        <h2 class="mb-0"><%= user.name %></h2>
        <small class="text-muted">Genres: <%= user.interests.join(", ") %></small>
      </div>
    </div>

    <h4 class="mb-3">Book Collection</h4>
    <table class="table table-dark table-striped">
      <thead>
        <tr><th>Title</th><th>Author</th><th>Rating</th></tr>
      </thead>
      <tbody>
        <% user.books.forEach(b => { %>
          <tr>
            <td><%= b.title %></td>
            <td><%= b.author %></td>
            <td>⭐ <%= b.rating %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
