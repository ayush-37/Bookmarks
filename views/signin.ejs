<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet"/>
    <style>
      body {
        background: linear-gradient(135deg, #fb6376, #fcb1a6);
        font-family: "Segoe UI", sans-serif;
        margin: 0;
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 2rem;
        box-sizing: border-box;
      }

      .signup-wrapper {
        max-width: 1000px;
        margin: auto;
        background-color: white;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        display: flex;
        flex-direction: row;
        overflow: hidden;
        min-height: 500px;
        animation: fadeIn 0.8s ease-in-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .info-section {
        background: white;
        flex: 1;
        padding: 2.5rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
        transition: all 0.3s;
      }

      .form-section {
        flex: 1;
        background-color: #f9f9f9;
        padding: 2.5rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      .form-section h2 {
        font-weight: 600;
        margin-bottom: 1.5rem;
      }

      .form-control {
        padding-left: 2.5rem;
      }

      .input-icon {
        position: absolute;
        top: 50%;
        left: 10px;
        transform: translateY(-50%);
        color: black;
      }

      .form-group {
        position: relative;
        margin-bottom: 1rem;
      }

      .btn-signup {
        background-color: #4b68ff;
        color: white;
        padding: 12px;
        width: 100%;
        border-radius: 10px;
        transition: background-color 0.3s ease;
      }

      .btn-signup:hover {
        background-color: #3a57d9;
      }

      .alt-signup-btn {
        border: 1px solid #ccc;
        border-radius: 10px;
        padding: 10px;
        width: 48%;
        transition: background-color 0.2s;
      }

      .alt-signup-btn:hover {
        background-color: #eaeaea;
      }

      .alt-options {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
      }

      .divider {
  display: flex;
  align-items: center;
  text-align: center;
  margin: 20px 0;
  color: #777;
  font-weight: 400;
}

.divider::before,
.divider::after {
  content: "";
  flex: 1;
  height: 1px;
  background: #ccc;
}

.divider::before {
  margin-right: 12px;
}

.divider::after {
  margin-left: 12px;
}


      .vertical-divider {
        width: 1px;
        background-color: #ddd;
        margin: 2rem 0;
      }

      @media (max-width: 768px) {
        .signup-wrapper {
          flex-direction: column-reverse;
        }
        .info-section,
        .form-section {
          padding: 1.5rem;
        }
        .vertical-divider {
          display: none;
        }
      }
    </style>
  </head>

  <body>
    <div class="signup-wrapper">
        <!-- Left info -->
      <div class="info-section text-center text-md-start">
        <h3 class="fw-bold">Fast, Efficient and Productive</h2>
        <p class="text-muted">
          Sign in to access personalized book recommendations and save your favorites.
        </p>
      </div>

      <!-- Vertical divider -->
      <div class="vertical-divider d-none d-md-block"></div>

      <!-- Right form -->
      <div class="form-section">
        <h2 class="text-center mb-4">Sign In</h2>
        <form action="/login" method="POST">
          <div class="form-group">
            <i class="fa fa-envelope input-icon"></i>
            <input
              type="email"
              class="form-control"
              id="email"
              name="email"
              placeholder="Email "
              required
            />
          </div>
          <div class="form-group">
            <i class="fa fa-lock input-icon"></i>
            <input
              type="password"
              class="form-control"
              id="password"
              name="password"
              placeholder="Password"
              required
            />
          </div>
          <p class="text-end mt-2">
  <a class="text-primary" href="/forgot-password">Forgot Password?</a>
</p>

          <div class="divider">or sign in with</div>

          <div class="alt-options mt-3">
  <button type="button" class="alt-signup-btn" onclick="window.location.href='/auth/google'">
    <i class="fab fa-google"></i> Google
  </button>
  <button type="button" class="alt-signup-btn" onclick="window.location.href='/auth/facebook'">
    <i class="bi bi-facebook"></i> Facebook
  </button>
</div>

          <button type="submit" class="btn btn-signup">Sign In</button>
          <p class="text-center mt-3 text-muted">
            Not a member! <a href="/signup">Sign Up</a>
          </p>
        </form>
      </div>
    </div>
  </body>
</html>
